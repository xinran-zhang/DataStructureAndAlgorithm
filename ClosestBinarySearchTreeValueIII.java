package grace.shuati;
import java.util.*;


// treat the BST as a int list
public class ClosestBinarySearchTreeValueIII {
    public List<Integer> closestKValues(TreeNode root, double target, int k) {
        List<Integer> res = new ArrayList<>();
        if (root == null || k == 0) {
            return res;
        }

        return res;
    }
}
